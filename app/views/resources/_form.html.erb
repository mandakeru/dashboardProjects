<%= simple_form_for(@resource) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :function %>
    <%= f.input :alocation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
